# Legacy inline frontend

This directory preserves the pre-React single-string DOM application for historical reference only. The production server serves the Vite build from src/control_plane/static; nothing imports this package.
